fastcgi_cache DockerAPP;

# What to cache: 'Code 200' responses, for half an hour.
fastcgi_cache_valid 200 30m;

# What to cache: only GET and HEAD requests (not POST).
fastcgi_cache_methods GET HEAD;
